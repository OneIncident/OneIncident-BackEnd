
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">
        <img src="/Assets/images/logoOne.png" width="250" height="auto" alt="logo">
      </a>
      <button class="navbar-toggler" type="button" 
      data-bs-toggle="collapse" 
      data-bs-target="#navbarSupportedContent" 
      aria-controls="navbarSupportedContent" 
      aria-expanded="false" 
      aria-label="Toggle navigation">

        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

          <% if (title =="Home"){%>
          <li class="nav-item">
            <%} else {%>
            <a class="nav-link active" aria-current="page" href="/"><i class="fas fa-regular fa-house"></i> Home </a>
          <%}%>
          </li>

          <% if (title =="Dashboard"){%>
          <li class="nav-item">
            <%} else {%>
            <a class="nav-link active" aria-current="page" href="/dashboard"><i class="fas fa-brands fa-elementor"></i> Incident Dashboard</a>
            <%}%>
          </li>

          <% if (title =="Record"){%>
          <li class="nav-item">
            <%} else {%>
            <a class="nav-link active" aria-current="page" href="/incident-list"><i class="fas fa-regular fa-layer-group"></i> Incident Record</a>
            <%}%>
          </li>
          

          <!-- TODO -->
           <% if(typeof displayName == 'string' && displayName) { %>
            
            <li class="navbar-text"><i class="fas fa-lg fa-user"></i>Welcome, <%= displayName %></li>
          <li class="nav-item"><a href="/logout" class="nav-link">
            <i class="fas fa-lg fa-sign-out-alt"></i> Logout</a></li>
          <% } else {%>
            <li class="nav-item"><a href="/login" class="nav-link">
              <i class="fas fa-lg fa-sign-in-alt"></i> Login</a></li>

            <% } %> 

      </div>
    </div>
  </nav>